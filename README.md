# seriesbuilder

Automation project for stepping through campaign creative settings and image-ad selection flows.

## Repository status

This repository was initialized as the preservation point before v2 work begins.

The previous Codex conversation referenced an `open-campaign.js` workflow for:

- waiting for the creative settings button
- selecting `이미지 광고` step by step
- verifying `span[data-surface-wrapper="1"]`

At initialization time, the GitHub repository was empty, so the original working source should be restored here if it exists on another local machine or workspace.

## Versioning plan

- `main`: stable preserved baseline
- `v2`: active modernization and feature work

