'use strict';

/**
 * v2 entrypoint placeholder.
 *
 * Restore the previous open-campaign workflow here, then move stable helpers into
 * ./lib and selectors into ./selectors as the implementation matures.
 */

async function runOpenCampaignV2() {
  throw new Error('open-campaign v2 is not implemented yet. Restore the v1 source before adding v2 behavior.');
}

module.exports = {
  runOpenCampaignV2,
};
